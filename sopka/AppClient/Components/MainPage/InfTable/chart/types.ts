export enum IncidentStatisticPeriodType {
    Week = "Week",
    Month = "Month",
    Period = "Period"
}

export enum IncidentStatisticGroupType {
    Day = "Day",
    AttackType = "AttackType"
}